import { InterfaceLocaliceDir } from "./localizeDir.interface";
import * as responses from "../locales/responses";

let localize_directory: InterfaceLocaliceDir = responses;

export default { localize_directory };
